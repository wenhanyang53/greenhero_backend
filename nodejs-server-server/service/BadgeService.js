'use strict';


/**
 * Create a new badge
 *
 * body Badge To create a new badge
 * no response value expected for this operation
 **/
exports.createBadge = function(body) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Delete badge
 * To delete a badge
 *
 * badgeName String The badgeName of the badge that needs to be deleted
 * no response value expected for this operation
 **/
exports.deleteBadge = function(badgeName) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Get badges
 * See the available badges
 *
 * body Badge see the badges
 * no response value expected for this operation
 **/
exports.getAllBadge = function(body) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Modify badge
 * To modify a badge 
 *
 * badgeName String The badgeName of the badge that needs to be modified
 * body Badge Updated badge object
 * no response value expected for this operation
 **/
exports.modifyBadge = function(badgeName,body) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

